{
  "name": "cocodrilo",
  "version": "0.0.1",
  "description": "The smallest UI framework ever",
  "main": "index.js",
  "scripts": {
    "test": "npx vitest",
    "format": "npx prettier --write .",
    "build": "npx esbuild index.ts --outfile=index.js",
    "size": "npx esbuild index.ts --bundle --minify | gzip -c | wc -c"
  },
  "author": "Vladimir Klepov v.klepov@gmail.com",
  "license": "MIT",
  "devDependencies": {
    "@testing-library/jest-dom": "^5.17.0",
    "@vitest/coverage-v8": "^0.34.1",
    "esbuild": "^0.18.11",
    "happy-dom": "^10.9.0",
    "prettier": "^3.0.1",
    "typescript": "^4.9.5",
    "vite": "^4.4.9",
    "vitest": "^0.34.1"
  }
}
