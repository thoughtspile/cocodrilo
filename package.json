{
  "name": "cocodrilo",
  "version": "0.0.1",
  "description": "The smallest UI framework ever",
  "main": "index.js",
  "scripts": {
    "build": "npx esbuild index.ts --outfile=dist/index.js",
    "size": "npx esbuild index.ts --bundle --minify | gzip -c | wc -c"
  },
  "author": "Vladimir Klepov v.klepov@gmail.com",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.18.11",
    "typescript": "^4.9.5"
  }
}
